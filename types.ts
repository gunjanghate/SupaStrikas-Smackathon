export type FormData = {
  eventId: string;
  eventName: string;
  eventDate: string;
  venue: string;
  seatNumber: string;
  price: string;
  bannerImage: string | File | null;
  tokenURI: string;
};
